# ── 密鑰：絕對不能進版控 ──────────────────────────────
# Anthropic API 金鑰，等同密碼且按用量計費
api_key.txt
.env
.env.*
!.env.example
*.pem
*.key

# ── Python ────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── Node（測試用的 puppeteer / tesseract 環境）──────
node_modules/
package-lock.json

# ── 發布封存：單檔 17MB，不進版控 ────────────────────
# 各版 zip 保留在本機 版本封存/ 資料夾，發布時另行提供
版本封存/*.zip

# ── OCR 暫存 ──────────────────────────────────────
adocr_*.img

# ── 作業系統 / 雲端同步 ────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 其他工具的本機設定：不進版控 ──────────────────
# Kiro 的 steering 規則寫「進入點為 docs/index.html」，與本專案的
# static/index.html 結構不符，留在 repo 會誤導後續協作者
.kiro/

# ── 內部商業評估資料：不公開 ────────────────────────
# 成本規劃、SWOT、商業模式圖、專案卡等內部文件，不進公開 repo
SPEC/課堂練習/

# 測試圖片不進版控。
# 理由有二：
#   1. 這些是廣告素材。合成的那幾張還好，但真實案例組會是實際廠商的廣告——
#      含第三方著作與代言人肖像，而這個 repo 是公開的，工具本身又是在標示違規，
#      把特定廠商的廣告放上去並不妥當。
#   2. 純二進位、更新頻繁，放進版控只會讓 repo 一直長大。
# 圖片留在本機 測試資料/ 底下照常使用；預期結果與判讀說明寫在
# 測試資料/測試資料說明.md（那份要進版控）。
測試資料/*.png
測試資料/**/*.png
測試資料/*.jpg
測試資料/**/*.jpg
測試資料/*.jpeg
測試資料/**/*.jpeg
測試資料/*.webp
測試資料/**/*.webp

# 主程式 exe 不進版控。
# 17 MB 的二進位，曾經佔 git 物件的 78%（clone 一次就要拉 17 MB），
# 而且自 v6 起就沒重建過——每個 tag 取出來的都是同一顆檔案，
# 「從 tag 重新建置」對它本來就不成立。
# 2026-08-28 以 git filter-repo 從全部歷史移除（23.9 MiB → 1.7 MiB）。
# 檔案留在本機照常使用；要發布請掛到 GitHub Releases 或 版本封存/ 的 zip。
違規廣告快篩.exe
*.exe
