# ── 密鑰：絕對不能進版控 ──────────────────────────────
# Anthropic API 金鑰，等同密碼且按用量計費
api_key.txt
.env
.env.*
!.env.example
*.pem
*.key

# ── Python ────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── Node（測試用的 puppeteer / tesseract 環境）──────
node_modules/
package-lock.json

# ── 發布封存：單檔 17MB，不進版控 ────────────────────
# 各版 zip 保留在本機 版本封存/ 資料夾，發布時另行提供
版本封存/*.zip

# ── OCR 暫存 ──────────────────────────────────────
adocr_*.img

# ── 作業系統 / 雲端同步 ────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 其他工具的本機設定：不進版控 ──────────────────
# Kiro 的 steering 規則寫「進入點為 docs/index.html」，與本專案的
# static/index.html 結構不符，留在 repo 會誤導後續協作者
.kiro/

# ── 內部商業評估資料：不公開 ────────────────────────
# 成本規劃、SWOT、商業模式圖、專案卡等內部文件，不進公開 repo
SPEC/課堂練習/
