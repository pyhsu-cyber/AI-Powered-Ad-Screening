# ── 密鑰：絕對不能進版控 ──────────────────────────────
# Anthropic API 金鑰，等同密碼且按用量計費
api_key.txt
.env
.env.*
!.env.example
*.pem
*.key

# ── Python ────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── Node（測試用的 puppeteer / tesseract 環境）──────
node_modules/
package-lock.json

# ── 發布封存：單檔 17MB，不進版控 ────────────────────
# 各版 zip 保留在本機 版本封存/ 資料夾，發布時另行提供
版本封存/*.zip

# ── OCR 暫存 ──────────────────────────────────────
adocr_*.img

# ── 作業系統 / 雲端同步 ────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 其他工具的本機設定（視需要決定是否納管）──────────
# .kiro/
